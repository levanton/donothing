{
  "name": "torch",
  "version": "0.1.0",
  "description": "Local Expo module that drives the camera torch (AVCaptureDevice) — used to blink the flash at session end while the phone lies face down.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "private": true
}
